apiVersion: v1
kind: Service
metadata:
  name: {{ include "stack-ai-chart.fullname" . }}
  labels:
    {{- include "stack-ai-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stack-ai-chart.selectorLabels" . | nindent 4 }}
